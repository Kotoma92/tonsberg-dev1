import { Routes } from '@angular/router';
import { VatVerifyComponent } from './vat-verify/vat-verify.component';
export const routes: Routes = [
    { path: 'vat-verify', component: VatVerifyComponent},
];
